---
import ProductLayout from '../layouts/ProductLayout.astro';
import ProductOrderSelector from '../components/product/ProductOrderSelector.astro';
import acrobatFiles from '../assets/acrobat-glide-path-files.png';
import etProduct from '../assets/et-transformx.png';
import ptProduct from '../assets/pt-transformx.png';
import bcsSealer from '../assets/bcs-sealer.png';
import vortiflowFlow from '../assets/vortiflow/vortiflow-fluid-dynamics-clean.png';
import { getShopifyStoreDomain } from '../utils/shopify';
import { buildOrderSelectorItems, getOrderCoverageLabel } from '../utils/productOrdering';
import { acrobatOrderEntries, etOrderEntries, ptOrderEntries } from '../data/productOrderingCatalog';

const base = import.meta.env.BASE_URL;
const shopifyStoreDomain = getShopifyStoreDomain();

const orderPanelDescription = `To order
- Choose file Size (Step 1)
- Choose file length (Step 2)
- Click 'Add to order'`;
const filePackFallbackPrice = '$65.00 NZD';

const acrobatSelectionNote =
  'The MB2 option is limited to 17 mm. Standard Acrobat sequence files are available in 21 mm, 25 mm, and 29 mm.';
const etSelectionNote =
  'ET TransformX currently covers .04 and .06 sequences across 21 mm, 25 mm, and 29 mm lengths, with assortment options available in the current NZ part-number range.';
const ptSelectionNote =
  'SX is supplied in 19 mm. Shaping and finishing files run across 21 mm, 25 mm, and 31 mm lengths, with PT assortment packs also available across the current NZ range.';

const [acrobatItems, etItems, ptItems] = await Promise.all([
  buildOrderSelectorItems(acrobatOrderEntries, shopifyStoreDomain, filePackFallbackPrice),
  buildOrderSelectorItems(etOrderEntries, shopifyStoreDomain, filePackFallbackPrice),
  buildOrderSelectorItems(ptOrderEntries, shopifyStoreDomain, filePackFallbackPrice),
]);

const quickLinks = [
  { label: 'Acrobat', href: '#acrobat-order' },
  { label: 'ET TransformX', href: '#et-order' },
  { label: 'PT TransformX', href: '#pt-order' },
  { label: 'BCS', href: '#contact-products' },
  { label: 'VortiFlow', href: '#contact-products' },
];

const upcomingFileFamilies = ['RECI X', 'Hand files', 'Additional rotary systems'];

const contactProducts = [
  {
    title: 'VortiFlow Irrigation Needle',
    text: 'Use the same page to move from shaping orders into cleaning-stage enquiries.',
    image: vortiflowFlow.src,
    href: `${base}products/vortiflow-irrigation-needle/`,
    cta: 'View VortiFlow',
  },
  {
    title: 'BCS BioCeramic Materials',
    text: 'Request availability for sealing and repair materials without leaving the ordering flow.',
    image: bcsSealer.src,
    href: `${base}products/bcs/`,
    cta: 'View BCS family',
  },
];

const orderModules = [
  {
    id: 'acrobat-order',
    eyebrow: 'Glide',
    title: 'Acrobat Glide Path',
    text: 'Build the glide path stage first, then continue directly into shaping on the same page.',
    image: acrobatFiles.src,
    alt: 'Acrobat Glide Path Files',
    orderTitle: 'Order the Acrobat sequence online',
    productFamily: 'Glide Path',
    productLabel: 'Acrobat Glide Path',
    items: acrobatItems,
    selectionNote: acrobatSelectionNote,
  },
  {
    id: 'et-order',
    eyebrow: 'Shape',
    title: 'ET TransformX',
    text: 'For clinicians already working in ET, EdgeX7, and .04/.06-style sequence logic.',
    image: etProduct.src,
    alt: 'ET TransformX rotary files',
    orderTitle: 'Order the ET sequence online',
    productFamily: 'Rotary Files',
    productLabel: 'ET TransformX',
    items: etItems,
    selectionNote: etSelectionNote,
  },
  {
    id: 'pt-order',
    eyebrow: 'Shape',
    title: 'PT TransformX',
    text: 'For ProTaper-style shaping workflows that need a more controlled apical response.',
    image: ptProduct.src,
    alt: 'PT TransformX rotary files',
    orderTitle: 'Order the PT sequence online',
    productFamily: 'Rotary Files',
    productLabel: 'PT TransformX',
    items: ptItems,
    selectionNote: ptSelectionNote,
  },
];
---

<ProductLayout
  title="NZ Ordering | EndoTech NZ"
  description="A single ordering page for Acrobat Glide Path, ET TransformX, and PT TransformX, with contact pathways for the wider EndoTech product family."
  image={ptProduct.src}
  accent="slate"
>
  <main class="order-page">
    <section class="order-hero" aria-labelledby="order-title">
      <div>
        <p class="order-eyebrow">NZ Ordering</p>
        <h1 id="order-title">Order here</h1>
        <p class="order-lede">
          Scroll from glide path to shaping without jumping between product pages. Acrobat, ET TransformX, and PT TransformX all use the same combined order logic.
        </p>
      </div>

      <div class="order-hero-side">
        <p>Selections made in any section stay in the same combined Shopify cart.</p>
        <nav class="order-quick-nav" aria-label="Order section shortcuts">
          {quickLinks.map((link) => (
            <a href={link.href}>{link.label}</a>
          ))}
        </nav>
      </div>
    </section>

    <section class="order-flow-note" aria-label="Ordering note">
      <div>
        <strong>One combined order</strong>
        <p>Add Glide Path, ET, and PT packs as you scroll. Each selector contributes to the same order state.</p>
      </div>
      <a href={`${base}resources/downloads/`}>Open technical files</a>
    </section>

    <section class="order-category-heading" aria-labelledby="file-systems-title">
      <div>
        <p class="order-eyebrow">File Systems</p>
        <h2 id="file-systems-title">Active ordering for glide path and shaping files.</h2>
        <p>
          Acrobat, ET TransformX, and PT TransformX are grouped here as the current file ordering area. This section is structured to expand as RECI X, hand files, and additional rotary systems are added.
        </p>
      </div>
      <div class="upcoming-family-list" aria-label="Upcoming file families">
        {upcomingFileFamilies.map((family) => (
          <span>{family}</span>
        ))}
      </div>
    </section>

    {orderModules.map((module, index) => (
      <section id={module.id} class="order-module" aria-labelledby={`${module.id}-title`}>
        <div class="order-module-card">
          <div class="order-module-index" aria-hidden="true">
            <span>{String(index + 1).padStart(2, '0')}</span>
            <small>{module.eyebrow}</small>
          </div>

          <div class="order-module-intro">
            <div>
              <p class="order-eyebrow">{module.eyebrow}</p>
              <h2 id={`${module.id}-title`}>{module.title}</h2>
              <p>{module.text}</p>
            </div>
            <figure>
              <img src={module.image} alt={module.alt} />
            </figure>
          </div>

          <ProductOrderSelector
            title={module.orderTitle}
            description={orderPanelDescription}
            coverageLabel={getOrderCoverageLabel(module.items)}
            packFormat="6 files per pack"
            items={module.items}
            shopifyStoreDomain={shopifyStoreDomain}
            selectionNote={module.selectionNote}
            productFamily={module.productFamily}
            productLabel={module.productLabel}
          />
        </div>
      </section>
    ))}

    <section id="contact-products" class="contact-products-section" aria-labelledby="contact-products-title">
      <div class="contact-products-heading">
        <div>
          <p class="order-eyebrow">Contact Products</p>
          <h2 id="contact-products-title">Materials and irrigation stay connected to ordering.</h2>
          <p>
            VortiFlow and BCS are included here as direct enquiry routes. Future obturation and bioceramic ordering modules can sit in their own product-family sections.
          </p>
        </div>
        <a href={`${base}about/contact/`}>Contact EndoTech NZ</a>
      </div>

      <div class="contact-products-grid">
        {contactProducts.map((product) => (
          <article>
            <figure>
              <img src={product.image} alt="" />
            </figure>
            <div>
              <h3>{product.title}</h3>
              <p>{product.text}</p>
              <a href={product.href}>{product.cta}</a>
            </div>
          </article>
        ))}
      </div>
    </section>
  </main>
</ProductLayout>

<style>
  .order-page {
    background: #f7f9fb;
    color: #0b1424;
  }

  .order-hero,
  .order-flow-note,
  .order-category-heading,
  .order-module,
  .contact-products-section {
    max-width: 1260px;
    margin: 0 auto;
    padding-left: 48px;
    padding-right: 48px;
  }

  .order-hero {
    display: grid;
    grid-template-columns: minmax(0, 0.95fr) minmax(320px, 0.78fr);
    gap: clamp(40px, 6vw, 84px);
    align-items: end;
    min-height: min(640px, calc(100vh - 82px));
    padding-top: 64px;
    padding-bottom: 56px;
    background:
      linear-gradient(rgba(15, 23, 42, 0.018) 1px, transparent 1px),
      linear-gradient(90deg, rgba(15, 23, 42, 0.018) 1px, transparent 1px);
    background-size: 34px 34px;
  }

  .order-eyebrow {
    margin: 0 0 18px;
    color: #64748b;
    font-size: 0.78rem;
    font-weight: 900;
    letter-spacing: 0.34em;
    text-transform: uppercase;
  }

  .order-hero h1,
  .order-category-heading h2,
  .order-module h2,
  .contact-products-heading h2,
  .contact-products-grid h3 {
    margin: 0;
    color: #090f1d;
    font-family: 'Sora', system-ui, sans-serif;
    letter-spacing: 0;
  }

  .order-hero h1 {
    max-width: 9ch;
    font-size: clamp(3.2rem, 4.8vw, 5.6rem);
    font-weight: 700;
    line-height: 1;
  }

  .order-lede,
  .order-hero-side p,
  .order-flow-note p,
  .order-category-heading p,
  .order-module-intro p,
  .contact-products-heading p,
  .contact-products-grid p {
    margin: 0;
    color: #536478;
    font-size: 1.04rem;
    line-height: 1.76;
  }

  .order-lede {
    max-width: 50rem;
    margin-top: 28px;
  }

  .order-hero-side {
    border-top: 1px solid #cfd9e2;
    padding-top: 26px;
  }

  .order-quick-nav {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin-top: 24px;
  }

  .order-quick-nav a,
  .order-flow-note a,
  .contact-products-heading > a,
  .contact-products-grid a {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 42px;
    border: 1px solid #c8d2dc;
    border-radius: 999px;
    padding: 0 16px;
    color: #0b1424;
    font-weight: 800;
    text-decoration: none;
    transition: border-color 180ms ease, background 180ms ease, transform 180ms ease;
  }

  .order-quick-nav a:hover,
  .order-quick-nav a:focus-visible,
  .order-flow-note a:hover,
  .order-flow-note a:focus-visible,
  .contact-products-heading > a:hover,
  .contact-products-heading > a:focus-visible,
  .contact-products-grid a:hover,
  .contact-products-grid a:focus-visible {
    transform: translateY(-1px);
    border-color: #9eb3c4;
  }

  .order-flow-note {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 24px;
    align-items: center;
    border-top: 1px solid #dbe3ea;
    border-bottom: 1px solid #dbe3ea;
    background: #ffffff;
    padding-top: 24px;
    padding-bottom: 24px;
  }

  .order-flow-note strong {
    display: block;
    margin-bottom: 6px;
    color: #0b1424;
    font-family: 'Sora', system-ui, sans-serif;
    font-size: 1rem;
  }

  .order-category-heading {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(280px, 0.42fr);
    gap: 36px;
    align-items: end;
    padding-top: 80px;
    padding-bottom: 16px;
  }

  .order-category-heading h2 {
    max-width: 17ch;
    font-size: clamp(2.35rem, 3vw, 3.75rem);
    line-height: 1.06;
  }

  .order-category-heading p {
    max-width: 52rem;
    margin-top: 20px;
  }

  .upcoming-family-list {
    display: grid;
    gap: 10px;
    border-top: 1px solid #cfd9e2;
    padding-top: 18px;
  }

  .upcoming-family-list span {
    display: flex;
    align-items: center;
    min-height: 42px;
    border: 1px solid #dbe3ea;
    border-radius: 999px;
    background: #ffffff;
    padding: 0 16px;
    color: #334155;
    font-size: 0.86rem;
    font-weight: 800;
  }

  .order-module {
    scroll-margin-top: 104px;
    padding-top: 40px;
    padding-bottom: 0;
  }

  .order-module + .order-module {
    padding-top: 84px;
  }

  .order-module-card {
    position: relative;
    overflow: hidden;
    border: 1px solid #ccd8e3;
    border-left: 7px solid #0f5878;
    border-radius: 10px;
    background:
      linear-gradient(90deg, rgba(15, 88, 120, 0.055), transparent 38%),
      #ffffff;
    padding: 34px 32px 32px;
    box-shadow:
      0 1px 0 rgba(255, 255, 255, 0.92) inset,
      0 26px 62px -52px rgba(15, 23, 42, 0.34);
  }

  .order-module-card::after {
    position: absolute;
    right: 0;
    bottom: 0;
    left: 0;
    height: 1px;
    background: #b7c6d4;
    content: '';
  }

  .order-module-index {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    min-height: 34px;
    margin-bottom: 28px;
    border: 1px solid #c8d5e0;
    border-radius: 999px;
    background: #f7fafc;
    padding: 0 14px 0 5px;
    color: #0b1424;
    font-family: 'Sora', system-ui, sans-serif;
    font-weight: 800;
  }

  .order-module-index span {
    display: grid;
    place-items: center;
    width: 24px;
    height: 24px;
    border-radius: 999px;
    background: #0b1424;
    color: #ffffff;
    font-size: 0.7rem;
  }

  .order-module-index small {
    color: #5076b8;
    font-size: 0.68rem;
    letter-spacing: 0.22em;
    text-transform: uppercase;
  }

  .order-module-intro {
    display: grid;
    grid-template-columns: minmax(0, 0.98fr) minmax(280px, 0.72fr);
    gap: 36px;
    align-items: end;
    margin-bottom: 28px;
  }

  .order-module-intro h2 {
    font-size: clamp(2rem, 2.5vw, 3rem);
    line-height: 1.06;
  }

  .order-module-intro p {
    max-width: 44rem;
    margin-top: 18px;
  }

  .order-module-intro figure {
    display: grid;
    place-items: center;
    min-height: 190px;
    margin: 0;
    border: 1px solid #d6e0e8;
    border-radius: 8px;
    background:
      linear-gradient(rgba(15, 23, 42, 0.022) 1px, transparent 1px),
      linear-gradient(90deg, rgba(15, 23, 42, 0.022) 1px, transparent 1px),
      #f8fafc;
    background-size: 22px 22px;
    padding: 26px;
  }

  .order-module-intro img {
    display: block;
    width: 100%;
    max-height: 240px;
    object-fit: contain;
    margin: 0 auto;
  }

  .order-module-card :global([data-order-selector]) {
    border-bottom: 0;
    background: transparent;
  }

  .order-module-card :global([data-order-selector] > div) {
    max-width: none;
    padding: 0;
  }

  .order-module-card :global(.order-selector-shell) {
    border-color: #cbd8e2;
    border-radius: 8px;
    background: rgba(248, 250, 252, 0.84);
    box-shadow: 0 28px 62px -52px rgba(15, 23, 42, 0.38);
  }

  .contact-products-section {
    border-top: 1px solid #dbe3ea;
    padding-top: 96px;
    padding-bottom: 112px;
    background: #ffffff;
  }

  .contact-products-heading {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 24px;
    align-items: end;
  }

  .contact-products-heading h2 {
    max-width: 15ch;
    font-size: clamp(2rem, 2.6vw, 3.15rem);
    line-height: 1.08;
  }

  .contact-products-heading p {
    max-width: 48rem;
    margin-top: 18px;
  }

  .contact-products-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 18px;
    margin-top: 42px;
  }

  .contact-products-grid article {
    display: grid;
    grid-template-columns: 220px minmax(0, 1fr);
    gap: 22px;
    border: 1px solid #dbe3ea;
    border-radius: 18px;
    background: #f8fafc;
    padding: 22px;
  }

  .contact-products-grid figure {
    display: grid;
    place-items: center;
    margin: 0;
    border-radius: 14px;
    background: #ffffff;
    padding: 16px;
  }

  .contact-products-grid img {
    width: 100%;
    max-height: 150px;
    object-fit: contain;
  }

  .contact-products-grid div {
    display: grid;
    align-content: start;
    gap: 12px;
  }

  .contact-products-grid h3 {
    font-size: 1.4rem;
    line-height: 1.18;
  }

  .contact-products-grid a {
    width: fit-content;
    margin-top: 6px;
  }

  @media (max-width: 1120px) {
    .order-hero,
    .order-flow-note,
    .order-category-heading,
    .order-module,
    .contact-products-section {
      padding-left: 30px;
      padding-right: 30px;
    }

    .order-hero,
    .order-category-heading,
    .order-module-intro,
    .contact-products-heading,
    .contact-products-grid {
      grid-template-columns: 1fr;
    }

    .order-hero {
      min-height: auto;
      padding-top: 48px;
      padding-bottom: 52px;
    }

    .order-flow-note {
      grid-template-columns: 1fr;
    }

    .contact-products-grid article {
      grid-template-columns: 200px minmax(0, 1fr);
    }
  }

  @media (max-width: 720px) {
    .order-hero,
    .order-flow-note,
    .order-category-heading,
    .order-module,
    .contact-products-section {
      padding-left: 18px;
      padding-right: 18px;
    }

    .order-hero h1 {
      font-size: clamp(2.8rem, 11.8vw, 4.2rem);
      line-height: 0.98;
    }

    .order-quick-nav {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .order-flow-note,
    .order-category-heading,
    .contact-products-section {
      padding-top: 20px;
      padding-bottom: 20px;
    }

    .order-module + .order-module {
      padding-top: 56px;
    }

    .order-module-card {
      border-left-width: 5px;
      padding: 22px 16px 18px;
    }

    .order-module-index {
      margin-bottom: 22px;
    }

    .order-module-intro {
      gap: 24px;
      margin-bottom: 22px;
    }

    .order-module-intro figure {
      min-height: 160px;
      padding: 18px 0;
    }

    .contact-products-grid {
      gap: 16px;
    }

    .contact-products-grid article {
      grid-template-columns: 1fr;
      padding: 18px;
    }

    .contact-products-grid a,
    .contact-products-heading > a {
      width: 100%;
    }
  }
</style>
