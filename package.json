{
  "name": "endotech-homepage",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "check:quote-catalog": "node scripts/validate-quote-catalog.mjs",
    "check:quote-types": "tsc --noEmit --target ES2022 --module ESNext --moduleResolution Bundler --lib ES2022,DOM,DOM.Iterable --strict --skipLibCheck src/lib/quoteRequest.ts src/lib/quoteRequestPdf.ts src/lib/quoteRequestSubmission.ts src/scripts/quote-request-builder.ts src/data/nzLaunchCatalog.ts",
    "check:docs-links": "node scripts/validate-docs-links.js",
    "build": "npm run check:quote-catalog && npm run check:quote-types && npm run check:docs-links && astro build && node scripts/validate-all-links.js",
    "build:staging": "npm run check:docs-links && astro build && node scripts/setup-password-protection.js && node scripts/validate-all-links.js",
    "build:github": "npm run check:docs-links && astro build && node scripts/validate-all-links.js",
    "preview": "astro preview",
    "astro": "astro",
    "export:laschal-catalogue": "node scripts/export-laschal-catalogue-pdf.mjs",
    "video:start": "npm --prefix endotech-video start",
    "video:render": "npm --prefix endotech-video render"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.37.6",
    "@tailwindcss/vite": "^4.2.1",
    "astro": "^5.6.1",
    "pdf-lib": "^1.17.1",
    "sharp": "^0.34.2",
    "starlight-links-validator": "^0.19.2",
    "tailwindcss": "^4.2.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.8",
    "jsdom": "^28.1.0",
    "playwright": "^1.58.2",
    "typescript": "^5.9.3"
  }
}
